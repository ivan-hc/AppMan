#!/bin/sh

cd /opt/bin
case "$1" in
  '') bash -x "appman" ;;
  'openshot') wget https://raw.githubusercontent.com/ivan-hc/AppMan/main/applications/openshot/install.sh ;
  	chmod a+x ./install.sh ; exec /opt/bin/install.sh ; rm /opt/bin/install.sh ;;
  *) exec /opt/bin/appman-install ;;
esac
