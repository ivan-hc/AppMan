#!/bin/sh

cd /opt/bin;
mkdir tmp;
cd ./tmp;
wget -r -A .AppImage -nd https://github.com/mmtrt/WINE_AppImage/releases/tag/continuous-devel;
cd ..;
mv ./tmp/wine*AppImage ./wine-devel;
chmod a+x /opt/bin/wine-devel;
echo "Visit https://github.com/mmtrt";
rm -R /opt/bin/tmp /opt/bin/wine-devel-installer
