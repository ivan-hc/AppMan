#!/bin/sh

cd /opt/bin;
mkdir /home/$USER/.android;
cd ~/.local/share; wget https://dl.google.com/android/repository/platform-tools-latest-linux.zip;
rm -R ./platform-tools;
unzip ./platform-tools-latest-linux.zip;
rm ./platform-tools-latest-linux.zip;
ln -s /home/$USER/.local/share/platform-tools/adb /opt/bin/adb;
ln -s /home/$USER/.local/share/platform-tools/fastboot /opt/bin/fastboot;
echo "";
echo "   This is the complete Android-SDK Platform Tools suite";
echo "                    'appman -i adb' ";
echo "to install a new and more updated version, when available ";
echo "";
rm /opt/bin/adb-installer
